#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Avoid CI failures when a newer Brakeman release exists.
# Opt-in by setting BRAKEMAN_ENSURE_LATEST=1.
ARGV.unshift("--ensure-latest") if ENV["BRAKEMAN_ENSURE_LATEST"] == "1"

load Gem.bin_path("brakeman", "brakeman")
